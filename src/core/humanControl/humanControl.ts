import Human from "../../engine/human/human";

export default class HumanControl {
    private human: Human
    
    constructor (
        human: Human
    ) {
        this.human = human
    }
    public webcamInput(movements: Array<string>) {

    }
}