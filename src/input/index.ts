export default class Input {
    private checks:Map<string, (degrees: Map<string, number>) => boolean> = new Map()
    constructor (

    ) {

    }
    public do(degrees: Map<string, number>) {

        // console.log(degrees)w
    }
}